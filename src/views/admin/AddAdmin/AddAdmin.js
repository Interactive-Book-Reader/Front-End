import React from 'react';

import PageContainer from 'src/components/container/PageContainer';

import { Typography } from '@mui/material';

const AddAdmin = () => {
    return (
        <PageContainer title="Add Admin Page" description="this is Add Admin Page">
            <Typography variant="h1">Add Admin Page</Typography>
        </PageContainer>
    );
}

export default AddAdmin;

